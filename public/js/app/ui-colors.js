// ============
// ui-colors.js
// ============
// 
// Build UI elements representing the available
// selectable pen colours.

$(function() {

  // var x, y = 0;
  // _.each(qd.options.pen.color, function(color, i) {
  //   // calculate the position
  //   x = center.x + (i % 2 == 0 ? -50 : 0);
  //   y = (i % 2 == 0 ? (i * 25) + 150 : y);
  //   // create the rect
  //   var rect = qd.ui.rect(x, y, 50, 50).attr({
  //     'fill'   : color,
  //     'stroke' : 'none'
  //   });
  //   // set cursor and control events
  //   rect.node.style.cursor = 'pointer';
  //   $(rect.node).on('mousedown touchstart', function(e) {
  //     e.stopPropagation();
  //   }).on('mouseup touchend mousemove touchmove', function(e) {
  //     if (!(qd._drawing || qd._dragging)) {
  //       e.stopPropagation();
  //       if (e.type == 'mouseup' || e.type == 'touchend') {
  //         qd.pen.color(rect.attr('fill'));
  //       }
  //     } else {
  //       rect.node.style.cursor = '';
  //     }
  //   });
  // });

});